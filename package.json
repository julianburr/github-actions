{
  "name": "github-actions",
  "version": "1.0.0",
  "description": "This repo contains a collection of reusable Github Actions.",
  "scripts": {
    "prepare": "husky install",
    "format": "prettier --write **/*.{md,json,yml}",
    "lint": "eslint . --quiet",
    "lint:staged": "lint-staged",
    "test": "echo TODO"
  },
  "lint-staged": {
    "**/*.js": "eslint --fix --quiet",
    "**/*.{md,json,yml}": "prettier --write"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "eslint": "^8.34.0",
    "eslint-config-prettier": "^8.6.0",
    "eslint-plugin-import": "^2.27.5",
    "eslint-plugin-prettier": "^4.2.1",
    "eslint-plugin-unused-imports": "^2.0.0",
    "husky": "^9.1.4",
    "lint-staged": "^15.2.9",
    "prettier": "^3.3.3"
  }
}
